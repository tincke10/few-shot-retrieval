# Web API layer (optional). Install only if you want the browser UI:
#   pip install -r api/requirements.txt
# The CLI does NOT need any of this.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # file uploads (ingest, added in a later iteration)
